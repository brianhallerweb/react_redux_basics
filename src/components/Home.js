import React from "react";

const Home = () => (
  <div>
    This is the home page of an app designed to demonstrate using React with
    React-Router and React-Redux.
  </div>
);

export default Home;
